# pyo3-abi3t

Test crate for the free-threaded stable ABI (`abi3t`) support in maturin.

Requires the `opaque-pyobject` branch of https://github.com/ngoldbaum/pyo3, which provides the
`abi3t` and `abi3t-py3XY` features that maturin tests detect.
