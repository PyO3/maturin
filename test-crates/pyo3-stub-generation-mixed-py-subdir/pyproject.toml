[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "pyo3-stub-generation-mixed-py-subdir"
dynamic = ["version"]

[tool.maturin]
module-name = "pyo3_stub_generation_mixed_py_subdir._pyo3_mixed"
python-source = "python"
