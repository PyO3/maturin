[package]
authors = ["konstin <konstin@mailbox.org>"]
name = "pyo3-pack"
version = "0.3.68"
description = "Build and publish crates with pyo3 bindings as python packages"
exclude = ["get-fourtytwo/**/*", "integration-test/**/*", "sysconfig/*"]
readme = "Readme.md"
repository = "https://github.com/pyo3/pyo3-pack"
license = "MIT OR Apache-2.0"
keywords = ["python", "cffi", "packaging", "pypi", "pyo3"]
categories = ["api-bindings", "development-tools::ffi", "command-line-utilities"]

[badges]
travis-ci = { repository = "PyO3/pyo3-pack" }
appveyor = { repository = "konstin/pyo3" }

[[bin]]
name = "pyo3-pack"

[lib]
name = "pyo3_pack"

[dependencies]
base64 = "0.9.3"
cargo_metadata = { git = "https://github.com/roblabla/cargo_metadata" }
digest = { version = "0.8.0", features = ["std"] }
failure = "0.1.3"
keyring = { version = "0.6.1", optional = true }
reqwest = { version = "=0.9.2", optional = true }
rpassword = "2.0.0"
serde_derive = "1.0.80"
serde_json = "1.0.32"
sha2 = "0.8.0"
structopt = "0.2.12"
target_info = "0.1.0"
toml = "0.4.8"
zip = "0.4.2"
serde = "1.0.80"
tar = { version = "0.4.17", optional = true }
libflate = { version = "0.1.18", optional = true }
human-panic = { version = "1.0.1", optional = true }
regex = "1.0.5"
indicatif = { rev = "0c3c0b5cbe666402ed7d3b9366c346f6eb0228fe", git = "https://github.com/mitsuhiko/indicatif" }
atty = "0.2.11"
tempfile = "3.0.4"
goblin = { version = "0.0.18", optional = true }
openssl = { version = "0.10.15", features = ["vendored"], optional = true }
pretty_env_logger = { version = "0.2.4", optional = true }
platforms = "0.1.4"

[dev-dependencies]
indoc = "0.2.8"

[features]
default = ["auditwheel", "upload", "log"]
auditwheel = ["goblin"]
upload = ["reqwest"]
password-storage = ["upload", "keyring"]
log = ["pretty_env_logger"]

# This will make rewquest use a statically linked version of openssl
musl = ["openssl"]

# sdists can be created, but they are currently useless (#2)
sdist = ["tar", "libflate"]

[workspace]
members = [
    "get-fourtytwo",
]

exclude = [
    # This is necessary due to https://github.com/eqrion/cbindgen/issues/203
    "points",
    # If we have this crate in the workspace, it will have pyo3 in it's deps and the bridge detection won't work
    "hello-world"
]
