[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "cffi-mixed"
dependencies = ["cffi; platform_python_implementation != 'PyPy'"]
dynamic = ["version"]

[tool.maturin]
bindings = "cffi"
