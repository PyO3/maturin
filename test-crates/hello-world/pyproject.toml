[build-system]
requires = ["maturin>=0.15,<0.16"]
build-backend = "maturin"

[tool.maturin]
bindings = "bin"

[[tool.maturin.targets]]
name = "hello-world"
bindings = "bin"

[[tool.maturin.targets]]
name = "foo"
