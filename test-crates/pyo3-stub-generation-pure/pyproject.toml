[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "pyo3-stub-generation-pure"
description = "Tests compilation of packages loading stubs generated by PyO3"
dynamic = ["version"]
